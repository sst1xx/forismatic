services:
  app:
    image: vitalplay/forismatic:latest
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
    environment:
      DB_PATH: /data/quotes.db
    restart: unless-stopped
    mem_limit: 256m
    cpus: "1.0"
